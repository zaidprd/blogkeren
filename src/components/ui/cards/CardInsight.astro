---
// Import necessary modules and utilities
import Icon from "@components/ui/icons/Icon.astro";

interface Props {
  insightEntry: {
    data: {
      title: string;
      slug: string;
      description?: string;
      cardImage: { url: string; altText: string };
      pubDate?: Date; // Tambahkan properti 'pubDate' ini
    };
  };
  insightLocale?: string;
  label?: string;
}

const {
  insightEntry,
  insightLocale,
  label = Astro.currentLocale === "fr" ? "Lire plus" : "Read more",
} = Astro.props;
---